name: Worker Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/worker-deploy.yml"
      - "apps/worker/**"
      - "packages/db/**"
      - "packages/shared/**"
      - "package.json"
      - "package-lock.json"

jobs:
  deploy-worker:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint worker
        run: npm run lint --workspace @the-platform/worker

      - name: Typecheck worker
        run: npm run typecheck --workspace @the-platform/worker

      - name: Build worker package
        run: npm run build --workspace @the-platform/worker

      - name: Set up Flyctl
        if: env.FLY_API_TOKEN != ''
        uses: superfly/flyctl-actions/setup-flyctl@master
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Deploy worker
        if: env.FLY_API_TOKEN != ''
        run: flyctl deploy --config apps/worker/fly.toml --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Skip deploy (no FLY_API_TOKEN)
        if: env.FLY_API_TOKEN == ''
        run: echo "Skipping deploy - FLY_API_TOKEN not configured"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
